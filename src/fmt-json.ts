// https://www.npmjs.com/package/format-json

/**
 * mode: diffy, plain, terse (default), space, lines
 * printConstructor: true | false (default) |
 * indentChar: one char or space
 * indentCount: chars per indent
 * avoidQuotes: boolean
 * maxStringLength: number
 * maxTextLength: number
 * maxArrayLength: number
 * maxObjectLength: number
 * cappedStringTail: number
 * cappedArrayTail: number
 * skipObject: Function
 * skipTypes: array of (typeof)
 */
export default class DummyClass {}
